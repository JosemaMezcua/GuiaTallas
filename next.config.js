/** @type {import('next').NextConfig} */
// Configuracion base de Next.js.
const nextConfig = {
  // Habilita avisos extra de React en desarrollo.
  reactStrictMode: true,
};

// Exporta la configuracion para el runtime de Next.js.
module.exports = nextConfig;
